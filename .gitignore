__pycache__/
*.pyc
.venv/
.pytest_cache/
day_planner_agent/.env
day_planner_agent/credentials.json
day_planner_agent/token.json
day_planner_backend_app/.env
day_planner_backend_internal/.env

# Terraform state can contain IAM bindings and resource metadata for the key
# that decrypts user refresh tokens.
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
# .terraform.lock.hcl is deliberately NOT ignored — committing it is what pins
# provider versions across machines and CI.
.adk/
*/.adk/day_planner_agent.tar.gz
day_planner_agent.tar.gz
*-providers.tf